/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { TooltipFilterTableDirective } from './tooltip-filter-table.directive';

describe('Directive: TooltipFilterTable', () => {
  it('should create an instance', () => {
    const directive = new TooltipFilterTableDirective();
    expect(directive).toBeTruthy();
  });
});
